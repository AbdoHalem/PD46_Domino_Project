namespace Connection.Engine.Router
{
    // Any class in your application that processes incoming network messages
    // MUST implement this interface, or the Router will ignore it.
    public interface IMessageHandler
    {
    }
}